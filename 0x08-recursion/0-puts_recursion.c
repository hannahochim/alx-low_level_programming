#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - prints a string
 * and a new line
 * @s: variable to print strings from
 *
 * Return: void
 */
void _puts_recursion(char *s)
{
	puts(s);
}
