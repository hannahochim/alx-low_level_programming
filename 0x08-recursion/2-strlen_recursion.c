#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - Funtion to return the
 * length of a string
 * @s: string variable
 * Return: string length
 */
int _strlen_recursion(char *s)
{
	int n;

	n = strlen(s);

	return (n);
}
