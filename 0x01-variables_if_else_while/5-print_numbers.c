#include <stdio.h>

/**
 * main - This code prints alphabets
 * in lower and upper case
 *
 * Description: Prints alphabet in lower casem then
 * in upper case, followed by a new line
 * Return: return 0 on success
 */

int main(void)
{
	int n;

for (n = 0; n < 10; n++)
{
	printf("%d", n);
}
printf("\n");
return (0);
}
